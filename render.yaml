services:
  - type: web
    name: bybit-p2p-resolution
    runtime: node
    plan: free
    buildCommand: echo "No build step"
    startCommand: node server.js
    healthCheckPath: /healthz
    envVars:
      - key: NODE_ENV
        value: production
